
pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/release' }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'extendrepo'
include 'users-service', 'accounts-service', 'authorization-service'
